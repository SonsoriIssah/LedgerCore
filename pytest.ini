[pytest]
asyncio_mode = auto
testpaths = tests
# The default pattern also matches *_test.py, which would sweep up
# tests/chaos_test.py -- a standalone script that fires 50 real transfers.
# Restricting this keeps it from running as a side effect of collection.
python_files = test_*.py
